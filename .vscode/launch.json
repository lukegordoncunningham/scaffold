{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Scaffold",
      "program": "${workspaceFolder}/bin/scaffold.js",
      "args": ["../default.yml"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}